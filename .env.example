# Paystack secret key — server-side only, never exposed to the browser.
# Used by app/api/give (initialize) and app/give/thanks (verify).
# Test keys start sk_test_, live keys sk_live_.
# Set this in the Hostinger project's environment variables.
PAYSTACK_SECRET_KEY=sk_test_replace_me
